cd(@__DIR__)
burrowactivate()
import ERA5Analysis as ERA
using CSV, DataFrames, StatsBase, Plots


function plot_seasonal_bias_rmsd_for_basin(eratype, basin)
    
end

function bias_plot(bias_data, title, fractions)